implement <Route>

Window package may have files with backslash in the name, simply renaming the path shall not work, one need to also look up for alternative.
	We may have packages flattened in one big folder with files with strange names.
